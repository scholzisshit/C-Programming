#include <stdio.h>

int main(){

    float r,pi,area;
    pi=3.147;

    printf("enter radius of the circle ");
    scanf("%f",&r);
    area=r*r*pi;
    printf("area of circle is %f",area);
    return 0;
}